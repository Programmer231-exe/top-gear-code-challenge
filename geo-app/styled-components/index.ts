import * as React from 'react';

export const StyledBox = React.lazy(() => import('./StyledBox'));
export const StyledIconButton = React.lazy(() => import('./StyledIconButton'));
export const PlaceholderDiv = React.lazy(() => import('./PlaceholderDiv'));
